#!/bin/bash
# Post-commit hook: Update documentation after each commit

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHON_SCRIPT="$SCRIPT_DIR/update_docs.py"

if [ -f "$PYTHON_SCRIPT" ]; then
    python3 "$PYTHON_SCRIPT" "$SCRIPT_DIR"
fi
